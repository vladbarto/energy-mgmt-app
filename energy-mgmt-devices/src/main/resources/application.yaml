database:
  user: ${DB_USER:postgres}
  name: ${DB_DBNAME:device-db}
  port: ${DB_PORT:5432}
  ip: ${DB_IP:localhost}
  password: ${DB_PASSWORD:postgres}
spring:
  application:
    name: energy-mgmt-devices
  datasource:
    url: jdbc:postgresql://${database.ip}:${database.port}/${database.name}
    username: ${database.user}
    password: ${database.password}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  rabbitmq:
    url: amqps://eoypmofr:EvcIW3cQhlCEeMARih3ltpjzja7lGN8j@cow.rmq2.cloudamqp.com/eoypmofr
    host: cow-01.rmq2.cloudamqp.com
    port: 5672
    username: eoypmofr
    password: EvcIW3cQhlCEeMARih3ltpjzja7lGN8j
    virtual-host: eoypmofr
    queue: device_to_monitor_queue
    exchange: device_to_monitor_exchange
    routing-key: "routing.key.#"
server:
  port: 6585
  servlet:
    context-path: '/api'
  error:
    whitelabel:
      enabled: false